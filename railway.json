{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-dev --optimize-autoloader && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "php artisan migrate --force || true && php artisan storage:link || true",
    "healthcheckPath": "/",
    "healthcheckTimeout": 300
  }
}
