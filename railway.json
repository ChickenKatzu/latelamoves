{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --ignore-platform-reqs && npm install && npm run build && php artisan optimize"
  },
  "deploy": {
    "startCommand": "php artisan serve --host=0.0.0.0 --port=$PORT",
    "healthcheckPath": "/",
    "healthcheckTimeout": 300
  }
}
